//CMissile.cpp
#include "stdafx.h" // must be added above all code in external .ccp files when using the template
#include "cvui.h" // for lab4 onward. To be included in .cpp file, not .h file
#include "CMissile.h"



//OG Code
/*

CMissile::CMissile(int radius, int lives = 1, cv::Point2f position = cv::Point2f(0, 0), cv::Point2f velocity = cv::Point2f(0, 0), cv::Size guiSize = cv::Size(1000, 1000)) :
   CGameObject(radius, lives, position, velocity, guiSize)
{}

*/